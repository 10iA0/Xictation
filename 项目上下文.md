# 项目上下文（AI 快速进入文档）

> 供 AI 新对话快速理解项目意图、当前状态和关键约束。完整需求见 `需求文档.md`，技术细节见 `开发文档.md`。

## 项目一句话

英语精听（听写）工具，FastAPI + Jinja2 SSR + PostgreSQL(Docker)，苹果风格 light 模式，多用户登录，部署在远程服务器。

## 当前状态

核心功能全部可用，已部署到 ECS 服务器。

### 已完成功能
- 用户系统：登录/登出、管理员用户管理、个人设置、API Key 管理
- 听写本：紧凑列表、搜索、创建、删除、拖拽排序
- 听写详情页：左右两列（听写卡片 + 分析卡片 + 结构卡片）、虚线连接线
- 听写卡片：听写+正确内容平级文本框、拖拽排序
- Diff 对比：LCS 单词粒度，连续红色融合，仅听写标红
- 正确内容标注：颜色（Notion 风格）、连读/弱读/爆破（划线+标签）、实时预览
- 分析卡片：读不懂/听不懂/听不到三区、DeepSeek 查询、去重合并
- 结构卡片：DeepSeek 翻译 + 手动分析
- 生词本：2 列网格、搜索、编辑、来源徽章（橙红/深蓝）
- 侧边栏可折叠（localStorage 持久化）
- 剪贴板：复制/剪切卡片

## 文件速查表

| 改什么 | 找哪个文件 |
|--------|-----------|
| 数据模型（含 User） | `app/models.py` |
| 认证工具（会话、密码） | `app/auth.py` |
| 认证路由（登录、用户管理、API Key） | `app/routers/auth.py` |
| API 路由（标注/生词/排序/翻译） | `app/routers/api.py` |
| 听写页面路由 | `app/routers/dictations.py` |
| 生词本页面路由 | `app/routers/vocabulary.py` |
| DeepSeek 集成 | `app/deepseek.py` |
| 数据库连接 | `app/database.py` |
| 应用入口 | `app/main.py` |
| 详情页前端（diff/标注/拖拽/结构卡片） | `app/static/js/detail.js` |
| 全局脚本（侧边栏/生词表单/拖拽） | `app/static/js/main.js` |
| 全局样式 | `app/static/css/style.css` |
| 登录页模板 | `app/templates/login.html` |
| 详情页模板 | `app/templates/dictation_detail.html` |
| 基础模板（含 CSS 版本号） | `app/templates/base.html` |
| 部署入口 | `Makefile` |
| Docker 编排 | `docker-compose.yml` |
| 环境配置 | `.env.test` / `.env.prod` |

## 关键约束

1. **服务端渲染**：Jinja2 SSR，不用前端框架
2. **苹果风格 light 模式**：不做暗色模式
3. **标注只标在正确内容上**：听写内容只做 diff 标红
4. **Diff 以正确内容为基准**：听写对不上的标红，正确内容不标红
5. **Docker 部署**：远程服务器，通过 Makefile 部署
6. **生词分类不由人工选择**：由调用上下文决定
7. **服务器 docker 需要 sudo**

## 踩过的坑

1. **SQLAlchemy JSON 不更新** → 必须手动 `flag_modified(obj, "sources")`
2. **标注 HTML 泄漏** → 分段渲染：先按标注边界切分，每段单独转义
3. **标注排版变形** → `inline-block` + `baseline` + 标签 `position: absolute`
4. **语音标签重复** → `labeledAnnIds` Set 去重
5. **×按钮时机** → 先移除 `annotation-preview-live` 类再渲染
6. **SQLAlchemy OR 括号** → `(A) | (B) | (C)` 每个条件加括号
7. **生词分类** → 最终方案：单记录 + `sources` 字典 `{cannot_read: [...], cannot_understand: [...]}`

## 缓存版本管理

修改 JS/CSS 后必须递增版本号（`?v=xxx`）：

- `base.html`: `style.css?v=45`, `main.js?v=16`
- `dictation_detail.html`: `detail.js?v=40`

## 部署

```bash
make test          # 部署到测试环境（18001）
make prod          # 部署到生产环境（18000）
make logs-test     # 查看测试日志
make logs-prod     # 查看生产日志
```

服务器：`ecs-user@118.31.109.63`，SSH Key: `/Users/jihang/Downloads/jihang.pem`

## 数据库操作

```bash
# SSH 到服务器后
sudo docker exec -it xictation-prod_db psql -U xictation -d xictation_prod
sudo docker exec -it xictation-test_db psql -U xictation -d xictation_test
```

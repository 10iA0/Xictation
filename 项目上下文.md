# 项目上下文（AI 快速进入文档）

> 本文档供 AI 阅读使用，目的是在新对话中快速理解项目意图、当前状态和关键约束，避免重复踩坑。
> 完整产品需求见 `需求文档.md`，完整技术细节见 `开发文档.md`。

## 项目一句话

基于网页的英语精听（听写）工具，FastAPI + Jinja2 SSR + PostgreSQL(Docker)，苹果风格 light 模式，单用户本地使用，全量 Docker 化部署。

## 当前状态

项目已完成 Demo 开发并完成 Docker 化，核心功能全部可用。

### 已完成功能
- 听写本：紧凑列表（48px 固定高度）、搜索、创建、删除、**拖拽排序**
- 听写详情页：左右两列 grid 布局（听写卡片 + 分析卡片 + 结构卡片）、**虚线连接线**
- 听写卡片：听写内容 + 正确内容平级文本框、保存/编辑切换、**拖拽排序**
- Diff 对比：LCS 单词粒度，标点独立分割，连续红色融合，仅听写标红
- 正确内容标注：颜色（字体+背景，Notion 风格选择器，含还原圆点）、连读/弱读/爆破（划线+标签）
- 标注实时预览、×删除（仅编辑模式）、持久化
- **分析卡片**：读不懂/听不懂/听不到三区、DeepSeek 查询、去重合并、**独立删除**
- **结构卡片**：翻译内容（DeepSeek 翻译）+ 分析内容（手动）、编辑/展示模式切换
- 生词本：2 列网格卡片、搜索、编辑、**来源徽章标签**（读不懂橙红/听不懂深蓝）
- **生词表单无人工分类选择**：分类由上下文决定
- 侧边栏可折叠（localStorage 持久化）
- **Docker 双环境**：测试（8001）+ 生产（8000），完全隔离

## 文件速查表

| 改什么 | 找哪个文件 |
|--------|-----------|
| 数据模型 | `app/models.py` |
| API 路由（标注/生词/卡片排序/翻译等） | `app/routers/api.py` |
| 听写页面路由（列表/详情/搜索） | `app/routers/dictations.py` |
| 生词本页面路由 | `app/routers/vocabulary.py` |
| DeepSeek 集成（lookup + translate） | `app/deepseek.py` |
| 数据库连接（支持环境变量） | `app/database.py` |
| 应用入口（环境判断 no-cache） | `app/main.py` |
| 详情页前端（diff/标注/拖拽/结构卡片） | `app/static/js/detail.js` |
| 全局脚本（侧边栏/生词表单/列表拖拽） | `app/static/js/main.js` |
| 全局样式 | `app/static/css/style.css` |
| 详情页模板 | `app/templates/dictation_detail.html` |
| 生词本模板 | `app/templates/vocabulary.html` |
| 首页模板（紧凑列表） | `app/templates/dictation_list.html` |
| 基础模板（含 CSS 版本号） | `app/templates/base.html` |
| Docker 编排 | `docker-compose.yml` |
| Docker 构建 | `Dockerfile` |
| 测试环境配置 | `.env.test` |
| 生产环境配置 | `.env.prod` |
| 依赖 | `requirements.txt` |

## 关键约束（不能改的决策）

1. **服务端渲染**：用 Jinja2 SSR，不用前端框架（React/Vue 等）
2. **单用户**：不做用户系统、不做权限控制
3. **苹果风格 light 模式**：不做暗色模式
4. **标注只标在正确内容上**：听写内容只做 diff 标红
5. **Diff 以正确内容为基准**：听写内容对不上的标红，正确内容不标红
6. **Docker 部署**：应用和数据库全部容器化，支持 test/prod 双环境
7. **生词分类不由人工选择**：表单中不含分类复选框，由调用上下文决定

## 踩过的坑（AI 必读，避免重复犯错）

### 1. SQLAlchemy JSON 字段不更新
- **场景**：修改 `vocabulary.sources`（字典格式）后 `commit` 不写入数据库
- **原因**：JSON 字段原地修改不被 SQLAlchemy 检测到
- **修复**：必须手动调用 `flag_modified(obj, "sources")` 或 `flag_modified(obj, "labels")`
- **涉及**：`api.py` 中 create_vocabulary、delete_dictation、delete_analysis_card

### 2. 标注 HTML 泄漏
- **场景**：标注显示 `background-color:null` 或 HTML 标签泄漏到页面
- **原因**：先转义整个文本再按 offset 切片，特殊字符导致 offset 错位
- **修复**：分段渲染——先按标注边界切分文本，每段单独转义再套 span

### 3. 标注排版变形
- **最终方案**：`display: inline-block` + `vertical-align: baseline` + 标签 `position: absolute`
- **关键**：横线在内层 `.annotation-text` 的 `border-bottom`，标签在外层 `.annotation-mark` 的 `position: absolute; bottom: 2px`

### 4. 语音标注标签重复显示
- **修复**：用 `labeledAnnIds` Set 跟踪，每个标注只在首次出现的段显示标签

### 5. ×按钮显示时机错误
- `saveCard()`：先移除 `annotation-preview-live` 类，再渲染标注
- `editCard()`：始终添加 `annotation-preview-live` 类 + 调用 `renderAnnotations`

### 6. Diff 标点导致误标
- **修复**：改用 `split(/(\s+|[^\w\s])/)` 将标点也作为独立 token

### 7. SQLAlchemy OR 运算符括号问题
- **场景**：filter 中用 `|` 连接多个条件时必须每个条件加括号
- **错误**：`A | B | C` → **正确**：`(A) | (B) | (C)`

### 8. 生词分类设计
- **最终方案**：单条记录 + `sources` 为字典 `{cannot_read: [...], cannot_understand: [...]}`
- **删除逻辑**：按 card_id + category 移除来源，两类都空则删除整条生词
- **UI**：生词本不显式分区，来源标签带颜色区分（读不懂橙红/听不懂深蓝）

## 缓存版本管理

修改 JS/CSS 后必须递增版本号，否则浏览器缓存旧文件：

- `base.html` 中 `style.css?v=xxx`
- `dictation_detail.html` 中 `detail.js?v=xxx`
- `base.html` 中 `main.js?v=xxx`

当前版本：style.css=v45, detail.js=v40, main.js=v16

## 部署方式

```bash
# 测试环境（端口 8001）
docker compose --env-file .env.test up -d

# 生产环境（端口 8000）
docker compose --env-file .env.prod up -d

# 重新构建（代码更新后）
docker compose --env-file .env.test up -d --build

# 查看日志
docker compose --env-file .env.test logs -f web

# 停止
docker compose --env-file .env.test down

# 完全重置（含数据）
docker compose --env-file .env.test down -v
```

## 数据库操作

```bash
# 进入测试环境数据库
docker exec xictation-test_db psql -U xictation -d xictation_test

# 进入生产环境数据库
docker exec xictation-prod_db psql -U xictation -d xictation_prod

# 查看生词（含 sources 字典结构）
SELECT id, word, phonetic, sources FROM vocabulary ORDER BY word;
```

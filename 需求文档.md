# 精听工具需求文档

## 一、项目概述

基于网页的英语精听（听写）工具，帮助用户通过听写练习提升英语听力能力。工具支持听写记录管理、错误改正与错误分析三大核心功能，并提供生词本与标签搜索能力。采用苹果风格 light 模式 UI，单用户本地使用。**全量运行在 Docker 上，支持测试和生产两套环境。**

## 二、页面结构

### 1. 首页（列表页）

页面分为左右两部分：

- **左侧栏（可折叠回收，状态持久化）**：导航名称为"听写本"和"生词本"
  1. **听写本**：主区域展示所有听写记录
  2. **生词本**：主区域展示所有生词
- **主区域**：根据侧栏选中项切换显示对应内容

### 2. 听写详情页

- 页面均等分为左右两列（grid 布局）
- **顶部信息区**：
  - 音频来源：以 URL 形式记录；若无 URL，则用文字描述音频来源
  - 标签：支持为本次听写添加多个标签（蓝色 chip 样式）
- **左列**：听写卡片区域
  - 每张听写卡片创建时即包含**听写内容**和**正确内容**两个平级文本框
  - 两个文本框宽度一致，右下角统一保存按钮
  - 卡片支持**拖拽排序**（⋮⋮ 拖拽手柄）
  - "+新建卡片"按钮放在最下面
  - 卡片默认显示展示模式（含 diff 对比），非编辑状态
- **右列**：分析卡片 + 结构卡片区域
  - 分析卡片（点击"分析错误"触发）：读不懂/听不懂/听不到三区
  - 结构卡片（点击"结构分析"触发）：翻译内容 + 分析内容

## 三、首页功能详述

### 1. 听写本（主区域）

- 以紧凑列表形式展示所有听写记录（固定高度 48px，一行一条）
- 每条记录展示：标题、创建时间、卡片数量、删除按钮
- **拖拽排序**：左侧 ⋮⋮ 手柄拖拽调整顺序
- 点击列表项可进入对应的听写详情页
- **搜索功能**：支持按以下范围搜索
  - 听写标题
  - 听写内容
  - 标签

### 2. 生词本（主区域）

- 以卡片形式展示所有生词（2 等分网格布局）
- **生词字段**：
  - 单词
  - 音标
  - 翻译
  - 备注（notes，可编辑）
  - 来源（按分类分别展示：读不懂来源 / 听不懂来源，带颜色标签）
    - 读不懂标签为橙红色 `#c93400`
    - 听不懂标签为深蓝色 `#0040a0`
  - 来源以圆角徽章标签形式展示，可点击跳转到对应听写页
- **分类不显式分区**：单词卡上不再有"读不懂"/"听不懂"徽章，颜色体现在来源标签上
- **搜索功能**：支持按单词搜索（含音标、翻译匹配）
- **编辑功能**：生词本中的单词支持编辑（音标、翻译、备注），分类由上下文决定无需人工选择
- **DeepSeek 集成**：添加生词时，点击按钮触发 DeepSeek API 自动查询音标和翻译
- **去重机制**：
  - 数据库层：word 字段唯一约束
  - API 层：合并标签和来源
  - 前端层：失焦自动搜索已有生词
- **清理机制**：当两类来源都空时自动删除该生词

## 四、详情页核心功能

### 功能一：听写卡片（左列）

#### 1. 卡片结构

- 每张听写卡片包含**听写内容**和**正确内容**两个平级文本框
- 两个文本框宽度一致，右下角统一保存按钮
- 卡片默认显示展示模式（含 diff 对比），非编辑状态
- 点击"编辑"按钮进入编辑模式，点击"保存"按钮回到展示模式

#### 2. Diff 对比

- **基准**：以正确内容为基准
- **粒度**：以单词为粒度对比
- **标红规则**：听写内容中对不上的词（空缺、写错、写多）用红色背景标注，正确内容正常显示
- **排版对齐**：保存后，听写内容和正确内容在排版上一致，便于逐词对比
- **连续融合**：连续的红色背景融合为一个整体，像一笔划过去一样
- **仅听写内容标红**：正确内容不标红

#### 3. 正确内容标注系统

正确内容下方保留标注按钮，支持以下标注类型：

##### 颜色标注
- **Notion 风格颜色选择器**：圆形色点，分字体行和背景行
- **字体颜色**：6 种预设颜色（橙、绿、蓝、红、紫、黄）
- **背景颜色**：6 种预设浅色背景
- **还原功能**：字体行末尾有黑色圆点（还原字体为默认黑色），背景行末尾有白色圆点（清除背景颜色）
- 点击还原圆点时，删除选中范围内同类型的已有颜色标注

##### 语音标注
- **连读**（蓝色）：选中文本下方划线，划线下方显示标签"连"
- **弱读**（紫色）：选中文本下方划线，划线下方显示标签"弱"
- **爆破**（橙红色）：选中文本下方划线，划线下方显示标签"爆"
- **显示顺序**：文字 → 横线 → 中文标签（从上到下）

##### 标注交互
- **实时预览**：编辑模式下，标注效果实时显示在文本框下方的预览区
- **×删除按钮**：只在编辑模式显示，保存后不显示
- **持久化**：所有标注保存到数据库，刷新或重新打开后仍能恢复显示
- **排版不影响文字**：有标注的行增加行距容纳标签，无标注的行保持正常行距

#### 4. 卡片排序

- **拖拽排序**：通过 ⋮⋮ 拖拽手柄拖拽调整顺序（HTML5 Drag & Drop API）
- "+新建卡片"按钮放在最下面

### 功能二：分析卡片（右列上方）

- **触发**：点击左列卡片的"分析错误"按钮（点击后按钮变灰禁用）
- **效果**：在右列出现一张分析卡片，与左列对应卡片关联
- **视觉关联**：左右卡片之间有虚线连接线
- **关闭**：右上角 × 按钮，关闭后"分析错误"按钮恢复可用
- **生词区域**：分析卡片含三个区域
  - **读不懂**：代表添加生词（不认识的单词）
  - **听不懂**：代表学习音标和重音位置（发音不熟悉的单词）
  - **听不到**：连读/弱读/爆破等语音现象（纯文本记录）
- **生词格式**：参考生词本中的单词卡（单词、音标、翻译、备注）
- **生词分类由上下文决定**：从哪个区域添加就属于哪个分类，表单中不需要人工选择
- **去重合并**：支持生词去重，已有生词合并标签和来源
- **独立删除**：同一单词同时在读不懂和听不懂中时，从一个分类删除不影响另一个
- **清理机制**：删除分析卡片或听写时，自动清理关联生词；两类来源都空时删除生词

### 功能三：结构卡片（右列下方）

- **触发**：点击左列卡片的"结构分析"按钮（点击后按钮变灰禁用）
- **效果**：在分析卡片下方出现结构卡片
- **视觉关联**：与分析卡片风格一致，同样有 ← 来自听写卡片 #N 的链接
- **关闭**：右上角 × 按钮，关闭后"结构分析"按钮恢复可用
- **翻译内容**：
  - 文本框 + "翻译"按钮（调用 DeepSeek 将正确内容翻译为中文）
  - 翻译按钮仅在编辑模式可见
- **分析内容**：
  - 纯文本框，手动输入分析内容
- **编辑/保存模式**：
  - 有内容时默认展示模式（不可修改），点击"编辑"进入编辑模式
  - 无内容时默认编辑模式
  - 保存后切换为展示模式

## 五、交互流程示意

```
首页
 │
 ├─ 左侧栏（可折叠，状态持久化）
 │   ├─ 听写本 ──> 主区域：紧凑列表（固定高度、拖拽排序、搜索）
 │   │                  │
 │   │                  └─ 点击新增/进入记录 ──> 听写详情页
 │   │                                                │
 │   │                                                ├─ 顶部：音频来源（URL/文字描述）+ 标签
 │   │                                                │
 │   │                                                ├─ 左列：听写卡片（拖拽排序）
 │   │                                                │     │
 │   │                                                │     ├─ 听写内容 + 正确内容（平级文本框）
 │   │                                                │     ├─ 保存后：单词粒度 diff 对比（仅听写标红）
 │   │                                                │     ├─ 正确内容标注：颜色 / 连读 / 弱读 / 爆破
 │   │                                                │     ├─ 工具栏：⋮⋮拖拽 | 分析错误 | 结构分析 | ×删除
 │   │                                                │     └─ "+新建卡片"按钮（最底部）
 │   │                                                │
 │   │                                                └─ 右列：分析卡片 + 结构卡片
 │   │                                                      │
 │   │                                                      ├─ 分析卡片（点击"分析错误"展开）
 │   │                                                      │   ├─ 读不懂生词区 → 添加生词
 │   │                                                      │   ├─ 听不懂生词区 → 添加生词
 │   │                                                      │   └─ 听不到记录区 → 添加记录
 │   │                                                      │
 │   │                                                      └─ 结构卡片（点击"结构分析"展开）
 │   │                                                          ├─ 翻译内容（DeepSeek 翻译）
 │   │                                                          └─ 分析内容（手动填写）
 │   │
 │   └─ 生词本 ──> 主区域：生词卡片网格（2列、搜索、编辑）
 │                    ├─ 字段：单词 / 音标 / 翻译 / 备注
 │                    ├─ 来源：读不懂(橙红) / 听不懂(深蓝) 徽章标签
 │                    ├─ DeepSeek 按钮：自动查询音标和翻译
 │                    └─ 编辑：音标、翻译、备注可编辑（分类不需选择）
```

## 六、部署架构

- **容器化部署**：应用 + 数据库全部运行在 Docker 容器中
- **双环境**：测试环境（端口 8001）+ 生产环境（端口 8000）
- **数据隔离**：两套环境使用独立的数据库和数据卷
- **配置管理**：通过 `.env.test` 和 `.env.prod` 环境文件管理差异配置
- **生产优化**：生产环境自动启用 HTTP 缓存，测试环境禁用缓存便于调试

## 七、其他说明

1. **音频播放**：详情页不嵌入音频播放器，仅记录音频来源（URL 或文字描述），用户自行打开链接
2. **标注持久化**：所有文本标注（颜色、连读、弱读、爆破）保存到数据库，刷新后恢复
3. **Diff 规则**：以正确内容为基准，听写内容中对不上的词用红色背景，连续红色背景融合为一笔
4. **用户系统**：暂不支持用户登录，单用户本地使用
5. **侧边栏**：可折叠回收，状态通过 localStorage 持久化，跨页面一致
6. **生词去重**：数据库唯一约束 + API 层合并标签 + 前端失焦自动搜索，三层机制
7. **生词来源追踪**：sources JSON 字典 `{cannot_read: [...], cannot_understand: [...]}` 存储多来源，按分类分开
8. **拖拽排序**：听写列表和听写卡片均支持 HTML5 拖拽排序

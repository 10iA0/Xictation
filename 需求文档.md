# 精听工具需求文档

## 一、项目概述

基于网页的英语精听（听写）工具，帮助用户通过听写练习提升英语听力能力。支持听写记录管理、错误改正与错误分析三大核心功能，并提供生词本与标签搜索能力。采用苹果风格 light 模式 UI，支持多用户登录。全量运行在 Docker 上，部署在远程服务器，支持测试和生产两套环境。

## 二、页面结构

### 1. 首页（列表页）

页面分为左右两部分：

- **左侧栏（可折叠回收，状态持久化）**：导航名称为"听写本"和"生词本"
  1. **听写本**：主区域展示所有听写记录
  2. **生词本**：主区域展示所有生词
- **主区域**：根据侧栏选中项切换显示对应内容

### 2. 听写详情页

- 页面均等分为左右两列（grid 布局）
- **顶部信息区**：
  - 音频来源：以 URL 形式记录；若无 URL，则用文字描述音频来源
  - 标签：支持为本次听写添加多个标签（蓝色 chip 样式）
- **左列**：听写卡片区域
  - 每张听写卡片创建时即包含**听写内容**和**正确内容**两个平级文本框
  - 两个文本框宽度一致，右下角统一保存按钮
  - 卡片支持**拖拽排序**（⋮⋮ 拖拽手柄）
  - "+新建卡片"按钮放在最下面
  - 卡片默认显示展示模式（含 diff 对比），非编辑状态
- **右列**：分析卡片 + 结构卡片区域
  - 分析卡片（点击"分析错误"触发）：读不懂/听不懂/听不到三区
  - 结构卡片（点击"结构分析"触发）：翻译内容 + 分析内容

## 三、首页功能详述

### 1. 听写本（主区域）

- 以紧凑列表形式展示所有听写记录（固定高度 48px，一行一条）
- 每条记录展示：标题、创建时间、卡片数量、删除按钮
- **拖拽排序**：左侧 ⋮⋮ 手柄拖拽调整顺序
- 点击列表项可进入对应的听写详情页
- **搜索功能**：支持按听写标题、听写内容、标签搜索

### 2. 生词本（主区域）

- 以卡片形式展示所有生词（2 等分网格布局）
- **生词字段**：单词、音标、词性、翻译、备注
- **来源**：读不懂(橙红 `#c93400`) / 听不懂(深蓝 `#0040a0`)，以圆角徽章标签展示，可点击跳转
- **搜索**：按单词搜索（含音标、翻译匹配）
- **编辑**：音标、翻译、备注可编辑，分类由上下文决定
- **DeepSeek 集成**：添加生词时自动查询音标和翻译
- **去重**：数据库唯一约束 + API 层合并标签 + 修改词名时自动合并到已有生词 + 前端失焦自动搜索
- **清理**：两类来源都空时自动删除该生词

## 四、详情页核心功能

### 功能一：听写卡片（左列）

- 每张卡片包含**听写内容**和**正确内容**两个平级文本框，右下角统一保存按钮
- 默认展示模式（含 diff 对比），点击"编辑"进入编辑模式
- **Diff 对比**：以正确内容为基准，单词粒度，听写中对不上的词红色背景，连续红色融合
- **正确内容标注**：
  - 颜色标注：Notion 风格选择器，6 种字体色 + 6 种背景色 + 还原圆点
  - 语音标注：连读(蓝)/弱读(紫)/爆破(橙红)，划线 + 中文标签
  - 标注实时预览，×删除仅编辑模式，持久化到数据库
- **拖拽排序**：⋮⋮ 手柄拖拽

### 功能二：分析卡片（右列上方）

- 点击"分析错误"按钮展开，按钮变灰禁用
- 三个区域：读不懂(生词)、听不懂(生词)、听不到(纯文本)
- 生词分类由上下文决定，支持去重合并和独立删除
- 关闭后恢复按钮，删除时自动清理关联生词

### 功能三：结构卡片（右列下方）

- 点击"结构分析"按钮展开
- 翻译内容（DeepSeek 翻译）+ 分析内容（手动填写）
- 编辑/展示模式切换

## 五、用户系统

- **登录/登出**：账号密码登录，会话 cookie（7 天有效期）
- **角色**：admin（管理员）/ user（普通用户）
- **管理员功能**：用户管理（创建/编辑/删除用户、分配角色）
- **个人设置**：修改显示名、账号、密码
- **API Key 管理**：每个用户可配置自己的 DeepSeek API Key
- **密码策略**：至少 8 位，必须含大小写字母 + 数字 + 特殊符号

## 六、部署架构

- **服务器**：ECS 云服务器（118.31.109.63）
- **容器化**：应用 + 数据库全部运行在 Docker 容器中
- **双环境**：测试环境（端口 18001）+ 生产环境（端口 18000），数据隔离
- **部署方式**：本地通过 Makefile + rsync + SSH 同步代码到服务器，服务器上 docker compose 构建
- **配置管理**：`.env.test` 和 `.env.prod` 环境文件管理差异配置
- **工作流**：本地修改 → `make deploy-test` 验证 → `make deploy-prod` 发布

## 七、其他说明

1. **音频播放**：不嵌入播放器，仅记录音频来源，用户自行打开链接
2. **标注持久化**：所有标注保存到数据库，刷新后恢复
3. **Diff 规则**：以正确内容为基准，听写对不上的词红色背景，连续融合
4. **用户系统**：支持多用户登录，管理员可管理用户
5. **侧边栏**：可折叠，localStorage 持久化
6. **生词去重**：数据库唯一约束 + API 合并 + 前端搜索，三层机制
7. **拖拽排序**：听写列表和听写卡片均支持 HTML5 拖拽

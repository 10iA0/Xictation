# 精听工具开发文档

## 一、技术架构

- **后端**：FastAPI（Python Web 框架）
- **前端渲染**：Jinja2 服务端渲染（SSR）
- **ORM**：SQLAlchemy 2.x
- **数据库**：PostgreSQL 15（Docker 容器）
- **认证**：itsdangerous 签名会话 + passlib/bcrypt 密码哈希
- **AI 集成**：DeepSeek API（查询单词音标翻译、翻译英文文本）
- **HTTP 客户端**：httpx（同步调用 DeepSeek API）
- **部署**：Makefile + rsync + SSH → 服务器 Docker Compose

### 依赖清单

```
fastapi==0.104.1
uvicorn[standard]==0.24.0
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
jinja2==3.1.2
python-multipart==0.0.6
pydantic==2.5.2
httpx==0.25.2
itsdangerous==2.1.2
passlib==1.7.4
bcrypt==4.1.2
```

### 部署架构

```
本地开发机                              服务器 (118.31.109.63)
──────────                             ──────────────────────
Makefile                               /home/ecs-user/app/xictation/
├── make test ──rsync──>               ├── docker-compose.yml
├── make prod ──rsync──>               ├── .env.test  (端口 18001)
│                                       ├── .env.prod  (端口 18000)
│                                       ├── Dockerfile
│                                       └── docker compose build & up
```

### 环境变量

| 变量 | 说明 | 测试环境 | 生产环境 |
|------|------|---------|---------|
| COMPOSE_PROJECT_NAME | 容器名前缀 | xictation-test | xictation-prod |
| DATABASE_URL | 数据库连接串 | ...@db:5432/xictation_test | ...@db:5432/xictation_prod |
| ENVIRONMENT | 运行环境 | test | production |
| DEEPSEEK_API_KEY | DeepSeek 密钥 | 空 | 必填 |
| APP_PORT | 应用端口 | 18001 | 18000 |
| DB_PORT | 数据库端口 | 未暴露 | 未暴露 |

## 二、项目结构

```
Xictation/
├── app/
│   ├── __init__.py
│   ├── main.py              # FastAPI 应用入口
│   ├── database.py          # 数据库连接配置
│   ├── models.py            # SQLAlchemy 数据模型（含 User）
│   ├── auth.py              # 认证工具（会话、密码校验）
│   ├── deepseek.py          # DeepSeek API 服务
│   ├── templating.py        # Jinja2 模板实例（全局复用）
│   ├── routers/
│   │   ├── api.py           # API 路由（标注、生词、卡片排序、翻译等）
│   │   ├── auth.py          # 认证路由（登录、用户管理、API Key）
│   │   ├── dictations.py    # 听写页面路由
│   │   └── vocabulary.py    # 生词本页面路由
│   ├── static/
│   │   ├── css/style.css
│   │   └── js/
│   │       ├── detail.js    # 详情页（diff、标注、拖拽、结构卡片）
│   │       └── main.js      # 全局（侧边栏、生词表单、拖拽排序）
│   └── templates/
│       ├── base.html
│       ├── login.html       # 登录页
│       ├── profile.html     # 个人设置
│       ├── users.html       # 用户管理（管理员）
│       ├── api_settings.html # API Key 设置
│       ├── dictation_list.html
│       ├── dictation_detail.html
│       └── vocabulary.html
├── docker-compose.yml
├── Dockerfile
├── Makefile                 # 部署入口
├── .env.test
├── .env.prod
├── requirements.txt
└── .dockerignore
```

## 三、数据模型

### 1. 用户表（users）
- id, username(唯一), display_name, password_hash, role(admin/user)
- deepseek_api_key(用户自己的 API Key), created_at

### 2. 听写记录表（dictation）
- id, title, audio_source, order, user_id(外键→users), created_at
- 关联：cards(一对多), tags(一对多)

### 3. 听写卡片表（dictation_card）
- id, dictation_id(外键), content, correct_content, translation, analysis
- card_order, analysis_card_visible, structure_card_visible, created_at
- 关联：annotations(一对多), analysis_card(一对一), vocabularies(一对多)

### 4. 文本标注表（text_annotation）
- id, card_id(外键), field, start_offset, end_offset, text_content
- annotation_type(font_color/bg_color/liaison/weak/burst), annotation_value

### 5. 分析卡片表（analysis_card）
- id, card_id(外键,唯一), created_at
- 关联：records(一对多)

### 6. 分析记录表（analysis_record）
- id, analysis_card_id(外键), category(cannot_understand/cannot_hear), content

### 7. 生词表（vocabulary）
- id, word, phonetic, pos(词性), past_tense, past_participle, translation, notes
- labels(JSON), sources(JSON: {cannot_read: [...], cannot_understand: [...]})
- user_id(外键→users), source_dictation_id, source_card_id
- 唯一约束：(user_id, word)

### 8. 标签表（tag）+ 听写-标签关联表（dictation_tag）

### 9. 剪贴板表（clipboard）
- id, user_id(外键,唯一), source_card_id, action(copy/cut)

## 四、API 接口

### 认证与用户
- `GET/POST /login` - 登录页/登录提交
- `GET /logout` - 登出
- `GET /profile` - 个人设置页
- `GET /users` - 用户管理页（管理员）
- `GET /api-settings` - API Key 设置页
- `GET/PUT/DELETE /api/user/api-key` - API Key 管理
- `PUT /api/profile` - 修改个人信息
- `POST/GET/PUT/DELETE /api/users[/{id}]` - 用户 CRUD（管理员）

### 听写卡片
- `GET /dictations/{id}` - 听写详情页
- `POST /api/cards` - 创建卡片
- `PUT /api/cards/{card_id}` - 更新卡片（含 translation/analysis）
- `DELETE /api/cards/{card_id}` - 删除卡片
- `PUT /api/dictations/{id}/cards/reorder` - 卡片排序

### 文本标注
- `POST /api/cards/{card_id}/annotations` - 创建标注
- `GET /api/cards/{card_id}/annotations` - 获取标注
- `DELETE /api/annotations/{ann_id}` - 删除标注

### 分析卡片
- `POST /api/cards/{card_id}/analysis` - 创建分析卡片
- `DELETE /api/cards/{card_id}/analysis` - 删除分析卡片（清理关联生词）

### 结构卡片
- `PUT /api/cards/{card_id}` - 保存翻译和分析
- `POST /api/cards/{card_id}/translate` - DeepSeek 翻译

### 生词
- `POST /api/vocabulary` - 创建生词（去重合并）
- `PUT /api/vocabulary/{id}` - 更新生词
- `DELETE /api/vocabulary/{id}` - 删除生词
- `DELETE /api/vocabulary/{id}/source` - 移除指定来源
- `GET /api/vocabulary/search?word=xxx` - 搜索生词
- `GET /api/vocabulary/lookup?word=xxx` - DeepSeek 查询

### 听写
- `DELETE /api/dictations/{id}` - 删除听写（清理关联生词）
- `PUT /api/dictations/reorder` - 听写列表排序

## 五、关键技术实现

### 1. Diff 算法（detail.js: diffWords）
- LCS 最长公共子序列，单词粒度
- `split(/(\s+|[^\w\s])/)` 分割，保留空格和标点
- 连续非 match 项合并为一个红色背景 span

### 2. 标注渲染（detail.js: renderAnnotations）
- 分段渲染：按标注边界切分文本，每段单独转义再套 span
- 颜色标注内层，语音标注外层
- `labeledAnnIds` Set 防止语音标签重复显示

### 3. 颜色选择器（detail.js: showColorPicker）
- Notion 风格：6 字体色 + 6 背景色 + 还原圆点
- 还原逻辑：API 层 `is_reset` 删除同类型已有标注

### 4. 认证系统（auth.py + routers/auth.py）
- 会话：itsdangerous URLSafeTimedSerializer 签名 cookie，7 天有效期
- 密码：passlib/bcrypt 哈希，强度校验（大小写+数字+特殊符号+8位）
- 权限：`require_user` / `require_admin` 依赖注入

### 5. SQLAlchemy JSON 字段
- 问题：JSON 字段原地修改后 `commit` 不写入
- 修复：手动 `flag_modified(obj, "sources")` / `flag_modified(obj, "labels")`
- 涉及：create_vocabulary、delete_dictation、delete_analysis_card

### 6. DeepSeek API（deepseek.py）
- `lookup_word(word)`: 查询音标、词性、翻译
- `translate_text(text)`: 英文翻译为中文
- httpx 同步调用，模型 `deepseek-v4-flash`

### 7. 拖拽排序（main.js + detail.js）
- HTML5 Drag & Drop API
- `getDragAfterElement()` 用 reduce 找最近元素
- dragend 时调用 reorder API

## 六、部署命令

所有命令在项目根目录执行：

```bash
make test          # 同步代码 → 构建并部署到测试环境（18001）
make prod          # 同步代码+配置 → 构建并部署到生产环境（18000）
make sync          # 仅同步代码
make sync-config   # 仅同步配置文件
make restart-test  # 重启测试环境
make restart-prod  # 重启生产环境
make logs-test     # 查看测试日志
make logs-prod     # 查看生产日志
```

服务器直接操作（需 SSH 登录）：

```bash
ssh -i /Users/jihang/Downloads/jihang.pem ecs-user@118.31.109.63
sudo docker ps                                          # 查看容器
sudo docker exec -it xictation-prod_db psql -U xictation -d xictation_prod  # 进入数据库
sudo docker compose --env-file .env.prod down -v        # 完全重置（⚠️ 删数据）
```

## 七、已知问题与修复记录

1. **SQLAlchemy JSON 不更新** → `flag_modified()` 修复
2. **标注 HTML 泄漏** → 分段渲染修复
3. **标注排版变形** → `inline-block` + `baseline` + `position: absolute`
4. **语音标签重复** → `labeledAnnIds` Set 去重
5. **×按钮时机错误** → 先移除 preview-live 类再渲染
6. **SQLAlchemy OR 括号** → 每个条件加括号
7. **生词分类迭代** → 最终方案：单记录 + sources 分类字典

## 八、缓存版本管理

修改 JS/CSS 后必须递增版本号（`?v=xxx`），否则浏览器缓存旧文件：

- `base.html`: `style.css?v=45`, `main.js?v=16`
- `dictation_detail.html`: `detail.js?v=40`

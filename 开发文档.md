# 精听工具开发文档

## 一、技术架构

- **后端**：FastAPI 0.104.1（Python Web 框架）
- **前端渲染**：Jinja2 3.1.2 服务端渲染（SSR）
- **ORM**：SQLAlchemy 2.0.23
- **数据库**：PostgreSQL 15（Docker 容器）
- **AI 集成**：DeepSeek API（查询单词音标和翻译、翻译英文文本）
- **HTTP 客户端**：httpx 0.25.2（同步调用 DeepSeek API）
- **部署方式**：Docker Compose（数据库 + 应用容器，支持 test/prod 双环境）
- **UI 风格**：苹果风格 light 模式，单用户本地使用

### 依赖清单

```
fastapi==0.104.1
uvicorn[standard]==0.24.0
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
jinja2==3.1.2
python-multipart==0.0.6
pydantic==2.5.2
httpx==0.25.2
```

### Docker 架构

```
docker-compose.yml          # 统一编排，通过 --env-file 切换环境
├── .env.test               # 测试环境变量（端口 8001，DB: xictation_test）
├── .env.prod               # 生产环境变量（端口 8000，DB: xictation_prod）
├── Dockerfile              # 应用镜像构建
└── services:
    ├── db (postgres:15)    # 数据库服务
    └── web (自定义)        # FastAPI 应用服务
```

### 环境变量

| 变量 | 说明 | 测试环境值 | 生产环境值 |
|------|------|-----------|-----------|
| DATABASE_URL | 数据库连接串 | postgresql://...@db:5432/xictation_test | postgresql://...@db:5432/xictation_prod |
| ENVIRONMENT | 运行环境标识 | test | production |
| DEEPSEEK_API_KEY | DeepSeek API 密钥 | 空（用默认值） | 必填 |
| DEEPSEEK_MODEL | DeepSeek 模型名 | 空（用默认 deepseek-v4-flash） | 可选 |
| APP_PORT | 应用映射端口 | 8001 | 8000 |
| DB_PORT | 数据库映射端口 | 5433 | 不暴露 |

### 数据库连接

- 开发环境：`postgresql://xictation:xictation123@localhost:5432/xictation`
- Docker 环境：通过环境变量 `DATABASE_URL` 自动注入

## 二、项目结构

```
Xictation/
├── app/
│   ├── __init__.py
│   ├── main.py              # FastAPI 应用入口（含环境判断 no-cache）
│   ├── database.py          # 数据库连接配置（支持环境变量）
│   ├── models.py            # SQLAlchemy 数据模型
│   ├── deepseek.py          # DeepSeek API 服务（lookup + translate）
│   ├── templating.py        # Jinja2 模板配置
│   ├── routers/
│   │   ├── api.py           # API 路由（标注CRUD、生词合并、卡片排序、翻译等）
│   │   ├── dictations.py    # 听写页面路由（列表、详情、搜索）
│   │   └── vocabulary.py    # 生词本页面路由
│   ├── static/
│   │   ├── css/style.css    # 全局样式
│   │   └── js/
│   │       ├── detail.js    # 详情页脚本（diff、标注、拖拽、结构卡片等）
│   │       └── main.js      # 全局脚本（侧边栏、生词表单、拖拽排序等）
│   └── templates/
│       ├── base.html        # 基础模板
│       ├── dictation_list.html  # 首页（紧凑列表、拖拽排序）
│       ├── dictation_detail.html # 听写详情页（左右两列 + 分析/结构卡片）
│       └── vocabulary.html  # 生词本页面（2列网格、来源徽章）
├── docker-compose.yml       # Docker 编排（db + web）
├── Dockerfile               # 应用镜像构建
├── .env.test                # 测试环境配置
├── .env.prod                # 生产环境配置
├── .dockerignore            # Docker 构建排除
├── requirements.txt         # Python 依赖
├── 需求文档.md
├── 开发文档.md
└── 项目上下文.md
```

## 三、数据模型

### 1. 听写记录表（dictation）
- id（主键）
- title（标题）
- audio_source（音频来源 URL 或文字描述）
- order（排序序号，新建时 max(order)+1）
- created_at（创建时间）
- 关联：cards（听写卡片，一对多）、tags（标签关联，一对多）

### 2. 听写卡片表（dictation_card）
- id（主键）
- dictation_id（外键，关联听写记录）
- content（用户听写内容）
- correct_content（正确内容）
- card_order（卡片顺序）
- translation（翻译内容，DeepSeek 翻译结果）
- analysis（分析内容，手动填写）
- created_at（创建时间）
- 关联：annotations（文本标注，一对多）、analysis_card（分析卡片，一对一）、vocabularies（生词，一对多）

### 3. 文本标注表（text_annotation）
- id（主键）
- card_id（外键，关联听写卡片）
- field（标注字段：content / correct_content）
- start_offset（起始偏移量）
- end_offset（结束偏移量）
- annotation_type（标注类型：font_color / bg_color / liaison / weak / burst）
- annotation_value（标注值：颜色值等，可为 null）
- created_at（创建时间）

### 4. 分析卡片表（analysis_card）
- id（主键）
- card_id（外键，关联听写卡片，唯一约束）
- created_at（创建时间）
- 关联：records（分析记录，一对多）

### 5. 分析记录表（analysis_record）
- id（主键）
- analysis_card_id（外键，关联分析卡片）
- category（分类：cannot_understand / cannot_hear）
- content（记录内容）
- created_at（创建时间）

### 6. 生词表（vocabulary）
- id（主键）
- word（单词，唯一约束 uq_vocabulary_word）
- phonetic（音标）
- translation（翻译）
- notes（备注）
- labels（标签 JSON 数组：cannot_read / cannot_understand）
- source_dictation_id（来源听写页，外键，已废弃保留兼容）
- source_card_id（来源卡片，外键，已废弃保留兼容）
- sources（来源 JSON 字典：`{cannot_read: [{dictation_id, dictation_title, card_id}], cannot_understand: [...]}`）
- category（已废弃，保留兼容）
- created_at（添加时间）

### 7. 标签表（tag）
- id（主键）
- name（标签名，唯一约束）

### 8. 听写-标签关联表（dictation_tag）
- id（主键）
- dictation_id（外键，关联听写记录）
- tag_id（外键，关联标签）
- 联合唯一约束：(dictation_id, tag_id)

## 四、API 接口

### 听写卡片
- `GET /dictations/{id}` - 听写详情页
- `POST /api/cards` - 创建听写卡片
- `PUT /api/cards/{card_id}` - 更新听写卡片内容（含 translation/analysis）
- `DELETE /api/cards/{card_id}` - 删除听写卡片
- `PUT /api/dictations/{dictation_id}/cards/reorder` - 卡片拖拽排序

### 文本标注
- `POST /api/cards/{card_id}/annotations` - 创建标注
  - 颜色还原逻辑：font_color=#000000 或 bg_color=transparent 时，删除选中范围内同类型的已有标注
- `GET /api/cards/{card_id}/annotations` - 获取标注列表
- `DELETE /api/annotations/{ann_id}` - 删除单个标注

### 分析卡片
- `POST /api/cards/{card_id}/analysis` - 创建分析卡片
- `DELETE /api/cards/{card_id}/analysis` - 删除分析卡片（同时清理关联生词）

### 结构卡片
- `PUT /api/cards/{card_id}` - 保存翻译和分析内容（translation + analysis 字段）
- `POST /api/cards/{card_id}/translate` - 调用 DeepSeek 翻译正确内容

### 生词
- `POST /api/vocabulary` - 创建生词（含去重合并逻辑，尊重 payload.labels）
- `PUT /api/vocabulary/{vocab_id}` - 更新生词（支持 card_id + categories 编辑场景）
- `DELETE /api/vocabulary/{vocab_id}` - 删除生词
- `DELETE /api/vocabulary/{word_id}/source` - 从指定卡片指定分类移除生词来源（两类都空则删除生词）
- `GET /api/vocabulary/search?word=xxx` - 搜索已有生词（去重检查）
- `GET /api/vocabulary/lookup?word=xxx` - DeepSeek 查询音标和翻译

### 听写
- `DELETE /api/dictations/{id}` - 删除听写（同时清理关联生词）
- `PUT /api/dictations/reorder` - 听写列表拖拽排序

## 五、关键技术实现

### 1. Diff 算法（detail.js: diffWords 函数）

- **算法**：LCS（最长公共子序列），以单词为粒度对比
- **连续融合**：连续的非 match 项（包括空格）合并为一个 diff 组，整个连续区域放在一个红色背景 span 中
- **排版对齐**：听写内容和正确内容在排版上一致，便于逐词对比
- **实现要点**：
  - 用 `split(/(\s+)/)` 分割文本，保留空格
  - LCS 动态规划求最长公共子序列
  - 回溯生成对齐序列
  - 合并连续 diff 项时，包含空白 match 项，使红色背景连续

### 2. 标注渲染（detail.js: renderAnnotations 函数）

- **分段渲染**：先按标注边界切分文本，每段单独转义再套标注 span，避免 HTML 泄漏
- **颜色与语音分离**：颜色标注作为内层，语音标注作为外层
- **标签去重**：用 `labeledAnnIds` Set 跟踪已显示标签的标注，每个语音标注只在首次出现的段显示标签
- **标签合并**：同一文本段有多个语音标注时，合并到一个 `annotation-mark` 中，标签并排显示

### 3. 颜色选择器（detail.js: showColorPicker 函数）

- **Notion 风格**：圆形色点，分字体行和背景行
- **字体颜色**：`["#e65100", "#2e7d32", "#0d47a1", "#c62828", "#6a1b9a", "#f57f17"]`
- **背景颜色**：`["#fff3e0", "#e8f5e9", "#e3f2fd", "#fce4ec", "#f3e5f5", "#fffde7"]`
- **还原圆点**：字体行末尾黑色圆点（`#000000`），背景行末尾白色圆点（`transparent`），中间有竖线分隔
- **还原逻辑**：API 层判断 `is_reset`，删除选中范围内同类型的已有标注，不新建

### 4. SQLAlchemy JSON 字段

- **问题**：JSON 字段（labels、sources）原地修改后不会被 SQLAlchemy 检测到，`commit` 不写入数据库
- **解决方案**：手动调用 `flag_modified(obj, "field")`
- **涉及场景**：
  - `create_vocabulary`：合并已有生词时
  - `delete_dictation`：删除听写时清理关联生词
  - `delete_analysis_card`：删除分析卡片时清理关联生词

### 5. DeepSeek API 集成（deepseek.py）

- **两个功能**：
  - `lookup_word(word)`: 查询单词音标和翻译（JSON 格式返回）
  - `translate_text(text)`: 将英文文本翻译为中文
- **调用方式**：httpx 同步调用
- **API**：`https://api.deepseek.com/v1/chat/completions`
- **模型**：`deepseek-v4-flash`（可通过环境变量覆盖）
- **配置**：API Key 通过环境变量 `DEEPSEEK_API_KEY` 配置

### 6. 拖拽排序（main.js + detail.js）

- **HTML5 Drag & Drop API**
- **听写列表**：`initDictationDragSort()` — list-item 设 draggable，dragstart/dragend/dragover 三事件
- **听写卡片**：`initCardDragSort()` — card-row 设 draggable，排除分析卡片/结构卡片/输入框/按钮
- **位置计算**：`getDragAfterElement()` 用 reduce 找到鼠标位置下方的最近元素
- **保存时机**：dragend 时调用 reorder API

### 7. 结构卡片（detail.js + dictation_detail.html）

- **toggleExtra(btn)**：点击"结构分析"按钮，展开右侧 structure-card，按钮变灰禁用
- **closeStructure(cardId)**：× 关闭结构卡片，恢复按钮状态
- **saveStructure(cardId)**：保存翻译+分析，切换为展示模式
- **editStructure(cardId)**：切换回编辑模式
- **initStructureEditMode/displayMode**：初始化编辑/展示状态
- **独立于 saveCard/editCard**：听写内容和翻译/分析的保存完全解耦

### 8. 生词表单（main.js + detail.js showVocabForm）

- **无人工分类选择**：表单中不含"读不懂"/"听不懂"复选框
- **分类由上下文决定**：
  - 从分析卡片添加：`addVocabFromAnalysis(cardId, dictId, label)` — label 由所在区域传入
  - 从生词本编辑：`labels` 使用 defaults 中传入的现有 labels
  - 提交时直接使用 defaults.labels，不做人工修改
- **submit 时**：`close({ ..., labels: defaults.labels || [] })`

### 9. 生词来源模型（sources JSON 字典）

```json
{
  "cannot_read": [
    {"dictation_id": 1, "dictation_title": "VOA慢速英语", "card_id": 5},
    {"dictation_id": 3, "dictation_title": "BBC新闻", "card_id": 12}
  ],
  "cannot_understand": [
    {"dictation_id": 1, "dictation_title": "VOA慢速英语", "card_id": 5}
  ]
}
```
- 同一单词可在两个分类中各有不同来源
- 删除某一分类的来源不影响另一分类
- 两类来源数组都为空时，该生词记录被删除

## 六、已知问题与修复记录

### 1. SQLAlchemy JSON 字段不更新
- **问题**：合并已有生词时，`sources` 和 `labels` JSON 字段原地修改后 `commit` 不写入数据库
- **修复**：添加 `flag_modified(obj, "sources")` 和 `flag_modified(obj, "labels")`
- **涉及**：create_vocabulary、delete_dictation、delete_analysis_card 三处

### 2. 标注 HTML 泄漏
- **问题**：旧代码先转义整个文本再按原始 offset 切片，特殊字符导致 offset 错位，出现 `background-color:null` 等 HTML 泄漏
- **修复**：改为分段渲染——先按标注边界切分文本，每段单独转义再套标注 span

### 3. 标注排版变形（多次迭代）
- **问题**：标注后文字变形，用户反馈"人类已经无法阅读"
- **最终方案**：`display: inline-block` + `vertical-align: baseline` + 标签 `position: absolute`，有标注的行由 `padding-bottom` 撑高

### 4. 语音标注标签重复显示
- **问题**：语音标注被颜色标注边界拆分成多段，每段都重复显示标签
- **修复**：用 `labeledAnnIds` Set 跟踪，每个语音标注只在首次出现的段显示标签

### 5. ×按钮在保存后仍显示
- **问题**：`saveCard()` 中先调 `renderAnnotations`（此时 `isEditing=true`），后才移除编辑类
- **修复**：先移除 `annotation-preview-live` 类，再调 `renderAnnotations`

### 6. 搜索报错（SQLAlchemy OR 运算符）
- **问题**：`|` 运算符要求每个条件加括号，缺少括号导致 Python 语法错误
- **修复**：给 filter 中每个 OR 条件都加上括号

### 7. 生词分类设计迭代
- **第一版**：每个 word + category 一条独立记录 → 用户反馈删除一个分类影响另一个
- **第二版**：单记录 + sources 扁平数组 + category 字段 → 用户反馈不需要显式分区
- **最终版**：单记录 + sources 分类字典 `{cannot_read: [], cannot_understand: []}` + 来源标签着色

## 七、缓存版本管理

前端静态资源通过 URL 参数 `v=xxx` 管理缓存，每次修改 JS/CSS 后递增版本号：

- `base.html`: `style.css?v=45`
- `dictation_detail.html`: `detail.js?v=40`
- `base.html`: `main.js?v=16`

当前版本：style.css=v45, detail.js=v40, main.js=v16
